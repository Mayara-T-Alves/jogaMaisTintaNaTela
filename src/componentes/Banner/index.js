import './Banner.css'

function Banner() {
    //JSX: É como o React transforma os elementos no DOM
    return (
        <header className="banner">
            <img src="/imagens/banner.png" alt="Banner principal da página do organo" />
        </header>
    )
}

export default Banner